{
  "dashboard": {
    "title": "Linux VM Monitoring",
    "panels": [
      {
        "type": "graph",
        "title": "CPU Usage",
        "targets": [
          {
            "expr": "rate(node_cpu_seconds_total[5m])",
            "legendFormat": "{{cpu}}",
            "refId": "A"
          }
        ]
      }
    ]
  }
}
